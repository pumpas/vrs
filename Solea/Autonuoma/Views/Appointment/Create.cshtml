@model Org.Ktu.Isk.P175B602.Autonuoma.Models.Appointment

@{
	ViewData["title"] = "Appointment creator";
}

<div class="main-content-header container-fluid d-flex d-row justify-content-between align-items-center">
	<pantrast class="title">@ViewData["title"]</pantrast>
</div>

<div class="container-fluid d-flex justify-content-center">
@using (Html.BeginForm())
{
	@Html.AntiForgeryToken()
	
	@Html.ValidationSummary(true, "", new { @class = "text-danger" })
	@*
	@Html.HiddenFor(m => m.Id)
	//@Html.HiddenFor(m => m.Currency)
	@Html.HiddenFor(m => m.Currency, new { htmlAttributes = new { @Value=100 } })

*@
	<fieldset class="border rounded shadow p-2">
		<div class="form-group">

			<div>
			<atskls>@Html.LabelFor(m => m.PatientId, new { @class = "control-label" }) </atskls>
			@Html.EditorFor(m => m.PatientId, new { htmlAttributes = new { @class = "form-control", @placeholder = "Enter username" } })
			@Html.ValidationMessage("patient_id"@*m => m.PatientId, "", new { @class = "text-danger" }*@)
			</div>
			<div>
			


<script>
$(document).ready(function() {
  // Get the Role and Specialty fields
  var roleField = $('#role-field');
  var specialtyField = $('#Specialty');

  // Hide the Specialty field by default
  specialtyField.hide();

  // Add an event listener to the Role dropdown menu
  roleField.change(function() {
    // Check if "Doctor" is selected
    if (roleField.val() === 'Doctor') {
      // Show the Specialty field
      specialtyField.show();
    } else {
      // Hide the Specialty field
      specialtyField.hide();
    }
  });
});

</script>


			 
		</div>
	</fieldset>

	<div class="container-fluid d-flex d-row justify-content-end">
		<button class="btn btn-warning"><i class="fa-solid fa-floppy-disk"></i> Save</button>
	</div>
}
</div>

<div class="main-content-footer container-fluid d-flex d-row justify-content-between align-items-center">
	<span class="title"></span>
	<span class="actions">
		<a href="http://localhost:5000/Question" class="btn btn-success"><i class="fa-solid fa-angles-left"></i> Back to list</a>
	</span>
</div>
